{
  "name": "edator-renderer",
  "version": "0.0.1",
  "description": "Deterministic FFmpeg renderer for Edator edit packs (the execution half — Claude makes the decisions, this executes them).",
  "type": "module",
  "license": "MIT",
  "bin": {
    "edator-render": "./render.js",
    "edator-transcribe": "./transcribe.js",
    "edator-envelope": "./envelope.js",
    "edator-validate": "./validate.js",
    "edator-preflight": "./preflight.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "render": "node render.js",
    "validate": "node validate.js",
    "test": "node --test"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
